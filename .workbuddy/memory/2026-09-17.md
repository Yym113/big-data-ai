# 2026-09-17

## Python + AI 四节课学习地图

- 需求：小玉（新闻传播学、零基础）计划 4 次课 × 90 分钟（上下各 45 min）= 6 小时学完 Python 核心语法，然后用 Python 学 AI 基础应用（情感分析、文本分类等）。
- 交付：`learning-materials/python-ai-learning-map.html`（沿用仓库既有 HTML 配色：米白底 #fafaf7 + 深蓝主色 #2c5a87 + 卡片分节）。
- 课程设计取舍：
  1. 语法/环境 + 变量/字符串/列表 → 字典 + for/if
  2. 函数 + 文件读写（CSV/UTF-8）→ pandas 筛选/排序/计数
  3. 正则 3 招 + 停用词 → jieba 分词 + 词频 + 词云
  4. SnowNLP 情感分析 → sklearn 朴素贝叶斯文本分类
- 明确排除项（写进文档）：类/OOP、装饰器、生成器、多线程异步、深度学习/Transformer。
- 文档含：全局路线图表格、每节上下半场拆分、可跑代码示例、每节"不学"清单、课后必做练习、报错自救流程（UnicodeDecodeError / ModuleNotFoundError / FileNotFoundError / 词云方块）、进阶三方向。
- 关键提醒（已写入文档）：SnowNLP 训练语料是电商评论，用于新闻语域有系统性偏差——建议小玉做「人工打分 vs 模型打分」对比作为传播学小论文选题。
- 状态：文件已生成，**尚未 commit / push**。

## 更正（当天）：改为 13 次课 · 纯 Python 基础

- 小玉澄清：**总共有 13 次课，AI 理论与应用是后面的课程**；当前阶段任务只是「掌握足够用的 Python 基础知识」。
- 处理：删除 `python-ai-learning-map.html`（4 次课含 AI 版），新建 `learning-materials/python-learning-map.html`。
- 新地图结构（13 次 × 90 min = 19.5 h）：
  - **阶段一 语言入门（课 1–4）**：环境+变量类型 / 字符串+列表 / 字典+元组集合+嵌套 / if+for+while
  - **阶段二 组织逻辑（课 5–8）**：函数 / 推导式+lambda+内置函数 / 文件读写 TXT·CSV·编码 / 异常处理+调试
  - **阶段三 文本与数据实战（课 9–13）**：正则 / 模块与包+pip+虚拟环境 / numpy / pandas / matplotlib+综合项目
- 文档含「够用的标准」6 条验收线、学/不学对照表、每课课后必做练习、报错自救对照表（7 类高频错误）、AI 阶段的知识衔接映射（说明这 13 课为什么这么选）。
- 教学取舍原则（写进文档）：判断标准只有一条——**后面 AI 阶段会不会直接用到**。
- 淘汰项：类/OOP、装饰器、生成器、多线程异步、数据库、Web 框架、pandas 多表 join/透视表、seaborn/plotly。

## 概念资料批量生成（15 份 + 目录页）

- 需求：写入仓库 → 用 `concept-unpacker` skill 生成第 1 个概念 → 后台批量生成其余全部 → 另建**目录页**方便建立概念间联系。
- 已提交：`238a320 docs(learning-materials): 新增 Python 基础学习地图（13 次课·新闻传播学零基础版）`。
- 交付清单（`learning-materials/`）：
  - `index.html` — **概念目录页**（我手写）。15 张概念卡片按三阶段分组、依赖主线图、**6 组跨概念联系**（引用即引用 / 类型决定运算 / 循环+条件+累加器 / 推导式是循环简写 / pandas 是 numpy+字典思想集合 / 文件读写是唯一入口）、常见问题。
  - `vars-and-types.html` — 第 1 份（我手写，作为后续 14 份的格式基准）。
  - 其余 14 份由子代理批量生成：strings / lists / dicts / conditionals / loops / functions / comprehensions / file-io / exceptions / regex / modules-and-packages / numpy / pandas / matplotlib。
- 统一规范：9 段结构（个人理解 / 学习目标 / 核心问题 / 结构化解释 / 应用场景 / 概念辨析 / 自测问题 / 可核查来源 / 核查记录）+ `.toc` 跳转 + 统一 CSS（沿用仓库米白+深蓝配色）+ 页脚三链接（目录 / 地图 / GitHub）。
- 参考来源：Python 官方中文文档（tutorial/*、library/*、faq、reference）、PEP 8、NumPy / pandas / matplotlib 官方文档，全部附真实 URL。
- ⚠️ **踩坑记录（重要教训）**：子代理批量生成 14 份时，**擅自使用了中文标识符**（变量名 `网址列表`/`标题`、函数名 `def 读数据()`、类名 `class 抓取失败()`）。这不符合真实编程习惯，零基础学生照抄会养成坏习惯。
  - 修正：分两轮子代理清理，共改约 100 处（函数/类/参数名一轮、普通变量名一轮），涉及 exceptions / regex / file-io / modules-and-packages / numpy / matplotlib / functions。
  - **保留合理的中文**：字符串字面量（`"热线线索.csv"`）、字典键与列名（`"标题"`）、pandas 命名聚合别名（`条数=("内容","size")`）、注释、正文说明。
  - 教训：**让子代理批量产出教学内容时，必须在提示词里明确「代码标识符一律用英文、中文只出现在字符串/注释/正文」**，否则它会把中文教学语境渗透进代码。
- matplotlib 的 Windows/macOS 中文字体族名（`Microsoft YaHei`/`SimHei`/`PingFang SC` 等）属**社区经验**，官方只给了 Linux 示例；文中已在正文与核查记录双重标注为非官方。
- 状态：15 份概念资料 + 目录页全部完成，已提交 `415694d`。

## 定位修正：去掉「新闻传播学」身份标签

- 小玉反馈：**不用强调「新闻传播学学生」**，这门课本身是「大数据与人工智能」课程。
- 处理范围（**区分身份定位 vs 示例数据**）：
  - ✅ 改：15 份资料的页头「适用：新闻传播学零基础」→「适用：人工智能与大数据课程 零基础」
  - ✅ 改：7 份资料的「应用场景（新闻传播学）」标题 → 「应用场景」（exceptions / file-io / matplotlib / modules-and-packages / numpy / pandas / regex），含 TOC 锚点与正文标题两处
  - ✅ 改：`python-learning-map.html` 的 title、副标题、页脚三处
  - ❌ **保留**：代码示例与描述里的新闻场景数据（新闻标题、来源、浏览量、`strings.html` 里 `s = "新闻传播学"` 的字符串示例、目录页 3 处描述）—— 这些是**内容数据不是身份标签**，改动成本高且会让零基础学生脱离具体语境。
- 教训：批量生成教学材料时，「身份定位」和「示例数据」要分开处理——前者是元信息该统一，后者不宜大改。
- 状态：已提交（见下一次 commit）。
